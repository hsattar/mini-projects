## This repository has  different branches for working/practising using different tools/technologies.

## Branhes:

### redux-undo
Using the redux undo package with a counter and to list example.

### firebase-auth
Using several different methods firebase provides to authenticate a user which takes them to their personal page.